"""
================================================================================
                      LangChain 学习路线图
================================================================================

一、学习顺序 (按编号运行即可)
--------------------------------------------------------------------------------

  01_hello_chain.py     →  30 分钟
     搞清楚: Model / Prompt / OutputParser 三者如何通过 | 串联
     掌握后: 能写一个 "接收输入 → LLM 处理 → 返回结果" 的基本程序

  02_prompt_template.py →  30 分钟
     搞清楚: 变量占位、角色设定、Few-shot 引导、历史消息管理
     掌握后: 能写出工程化的提示词，不是拼字符串

  03_output_parser.py   →  30 分钟
     搞清楚: 如何让 LLM 返回结构化的 JSON / Pydantic 对象
     掌握后: LLM 的输出可以给下游代码直接消费，不再需要正则解析

  04_rag.py             →  1 小时
     搞清楚: 文档加载 → 文本分割 → 向量化 → 存储 → 检索 → 生成 全链路
     掌握后: 能做"基于自己知识库的问答系统"，这是企业级最常见的需求

  05_agent.py            →  45 分钟
     搞清楚: 工具定义(@tool)、Agent 自动决策调用哪个工具
     掌握后: 能做"LLM 自动查天气、算数、调 API"的智能助手

  06_memory.py           →  45 分钟
     搞清楚: 无记忆 vs 有记忆、多 session 隔离、长对话摘要压缩
     掌握后: 能做多轮对话的聊天机器人


二、学完能到什么程度？
--------------------------------------------------------------------------------

  能力层级              具体能做什么
  ──────────────────────────────────────────────────────────────
  Level 1 (01-03)      写脚本自动处理文本、生成结构化数据
  Level 2 (04)         搭建企业知识库问答、合同审查、文档分析
  Level 3 (05)         智能客服自动处理、工单路由、多工具编排
  Level 4 (06)         带记忆的聊天机器人、个性化助手

  学完这 6 个文件，你就能独立做出:
    - 基于公司文档的智能问答机器人
    - 自动调用多工具的 Agent (查库 + 计算 + 发邮件)
    - 多轮对话的客服系统
    - 结构化信息提取流水线 (从 PDF/网页中提取字段)


三、实战进阶路径 (本目录之外)
--------------------------------------------------------------------------------

  学完基础后，按这个顺序深入:

  1. LangGraph 深入 (Agent 05 文件的底层引擎)
     - 自定义状态图 (StateGraph)
     - 条件分支和循环
     - 人工介入 (Human-in-the-loop)

  2. 向量数据库选型
     - Chroma (已在 04_rag.py 用过) → 轻量、本地
     - Milvus / Weaviate / Pinecone → 生产环境

  3. 生产级 RAG
     - 查询重写 (Query Rewriting)
     - 混合检索 (Hybrid Search: 关键词 + 向量)
     - 重排序 (Rerank)
     - 文档分级 (Parent-Child chunking)

  4. MCP (Model Context Protocol)
     - 用 MCP 给 Agent 接入更多标准工具
     - 数据库、文件系统、浏览器等

  5. LangServe / LangSmith
     - 部署 API
     - 追踪和调试调用链


四、学习建议
--------------------------------------------------------------------------------

  1. 每个文件先跑通，看效果
  2. 然后改参数/换模型，观察变化
  3. 最后尝试不用示例，自己写一个相似的功能
  4. 如果卡住，看 LangChain 官方文档: https://python.langchain.com
"""
